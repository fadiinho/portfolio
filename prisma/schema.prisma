generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Project {
  id String @id @default(uuid())

  name String
  github String
  screenshotUrl String?
  description String
  liveDemo String?
  stacks Json
}
